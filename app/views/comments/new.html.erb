<%# comments#new %>
<%= form_with model: Comment, url: user_post_comments_path, method: :post, local:true do |form| %>
    <div>
      <%= form.text_field :text, placeholder: "Add new comment" %>
    </div>

    <div>
      <%= form.submit "Submit", class: "btn-see-all" %><br>
    </div>
<% end %>